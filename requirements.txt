# Base
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.40.0

# Text processing
pdfplumber>=0.10.0
ebooklib>=0.18
markitdown[docx]>=0.1.0
python-docx>=1.1.0
beautifulsoup4>=4.12
lxml>=5.0
num2words>=0.5.12
# Расстановка ударений в русском тексте (омографы)
# https://github.com/Den4ikAI/ruaccent
ruaccent>=1.4.0

# ASR — GigaAM-v3 (trust_remote_code=True)
sentencepiece>=0.2.0
# hydra-core и omegaconf — только если используем оригинальный GigaAM репозиторий,
# для transformers-интеграции не требуются (проверить при реализации)

# ASR — Whisper (fallback)
# Встроен в transformers

# TTS — Qwen3-TTS (через transformers или qwen_tts пакет)
# pip install git+https://github.com/QwenLM/Qwen3-TTS.git

# TTS — F5-TTS (optional)
# f5-tts>=1.0.0

# Audio processing
pydub>=0.25.1
soundfile>=0.12.1
librosa>=0.10.0
noisereduce>=3.0.0
pyloudnorm>=0.1.1

# CLI + Config
typer>=0.12.0
rich>=13.0.0
pyyaml>=6.0
tqdm>=4.66.0
pydantic>=2.0.0          # Валидация config.yaml

# Dev
pytest>=8.0.0
pytest-cov>=4.1.0
ruff>=0.5.0              # Линтер
